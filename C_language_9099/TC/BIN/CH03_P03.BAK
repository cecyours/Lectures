#include<stdio.h>
#include<conio.h>
void main()
{
	int a,b;
clrscr();
	 printf("Enter 2 numbers : ");
	 scanf("%d,%d",&a,&b);

	 if(a==b)
	 {
		printf("Both are same..");
	 }
	 else
	 {
		printf("Both are different");
	 }

getch();
}
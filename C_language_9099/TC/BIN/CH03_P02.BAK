//relational operators

#include<stdio.h>
#include<conio.h>
void main()
{
	int a,b;
clrscr();

	printf("Enter 2 numbers : ");
	scanf("%d,%d",&a,&b);

	printf("%d > %d = %d\n",a,b,a>b); // 0 -> false, 1->true

	if(a>b)
	{
		printf("a = %d is max.",a);
	}
	else
	{
		printf("b = %d is max.",b);
	}
getch();
}
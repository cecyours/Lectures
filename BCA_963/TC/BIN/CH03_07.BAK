#include<stdio.h>
#include<conio.h>
void main()
{
	const float OTP = 871199;  //out of int range.
	float user_OTP;
clrscr();
	 printf("Enter the OTP : ");
	 scanf("%f",&user_OTP);

	 if(!(user_OTP == OTP))
	 {
		printf("Invalid OTP");
		getch(); // wait until user press any key.
		exit(0); //exit the program
	 }
	 printf("Welcome .... %.0f\n",user_OTP);
getch();
}
#include<stdio.h>
#include<conio.h>
void main()
{
	int a,b,ans;
clrscr();
	printf("Enter numbers : ");
	scanf("%d+%d",&a,&b);
	/* NOTE : here we use + to separate two %d,
	so runtime we have to enter + between 2 number. */

	ans = a+b;
	printf("    answer is : %d",ans);
getch();
}
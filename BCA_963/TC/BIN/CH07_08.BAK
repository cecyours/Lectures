#include<stdio.h>
#include<conio.h>
void main()
{
 int a=10;
 int *p;
   clrscr();
	p = &a; // assign address of a into p pointer.

	printf(" a -> Value : %d\t address : %p\n",a,&a);
	printf(" p -> Value : %d\t address : %p\n",*p,p); //to access value use * before it.
   getch();
}
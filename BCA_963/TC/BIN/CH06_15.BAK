#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
	char str[100],ch='q',*pos;
clrscr();
	printf("Enter a string : ");
	gets(str);
	pos = strchr(str,ch);

	if(pos)
	{
		printf(" '%c' is found at address %p\n",ch,pos);
	}
	else
	{
		printf(" '%c' is not found.\n",ch);
	}


getch();
}
#include<stdio.h>
#include<conio.h>
void main()
{     char str[100];
      int i;
clrscr();
       printf("Enter a String : ");
       gets(str);

       for(i=0;str[i];i++)
       {
		if(str[i]>='A' && str[i]<='Z')    //if character is in uppercase
		{
			str[i]=str[i]+32;
		}
       }
       printf("String %s\n",str);
getch();
}